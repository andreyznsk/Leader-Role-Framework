{
  "name": "java-memory-service-playwright",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright UI and browser interaction tests for JavaMemoryService",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "codegen": "playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
