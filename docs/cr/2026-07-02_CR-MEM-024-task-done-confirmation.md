# 2026-07-02_CR-MEM-024: Confirmation before moving a task to DONE

**Дата:** 2026-07-02
**Статус:** Implemented
**Сервис:** MEM / JavaMemoryService
**Тип:** Enhancement / UI

## Итог реализации

Реализовано и покрыто E2E-тестами:

- Чекбокс «Выполнено» на `/ui/today` (`toggleDone()`) запрашивает `window.confirm(...)` перед переводом задачи в `DONE`; при отмене статус не меняется.
- Явный выбор статуса `DONE` в форме редактирования задачи (`/ui/tasks/{id}/edit`) также запрашивает подтверждение перед сохранением, если исходный статус был другим; сохранение без изменения статуса подтверждения не требует.
- Обратный переход из `DONE` подтверждения не требует.

## Проблема / Мотивация

Случайный клик по чекбоксу/выбор статуса мог мгновенно закрывать задачу без возможности передумать.

## Решение

Точечное добавление `window.confirm(...)` в двух местах UI (`templates/today.html`, `templates/task-edit.html`), без изменений в backend/API — используются существующие эндпоинты `POST /api/tasks/{id}/toggle-done` и `PUT /api/tasks/{id}`.

## Изменения в API

Нет.

## Изменения в схеме БД

Нет.

## Тесты

`JavaMemoryService/test_e2e/tests/task-done-confirm.spec.js` — 5 сценариев (отказ/согласие в обоих местах + отсутствие лишнего подтверждения при сохранении без смены статуса). Все зелёные.
